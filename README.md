# WB_DataEngg_CaseStudy
#### Important

1. All the SQLs are using keywords as per Snowflake standards.

2. All the users (both registered and non registered) will have entry in 
   from page_impression.
   
3. Table materialization will be used to build the models in dbt and hence no 
   DMLs are generated the case study and can be produced if required.
 
